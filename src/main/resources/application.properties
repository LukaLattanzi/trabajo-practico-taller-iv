# Datos de conexión: ajusta URL, usuario y contraseña según tu entorno
spring.datasource.url=jdbc:mysql://localhost:3306/taller_iv_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mysql@Unlimited2077

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Opcional: formatear SQL en consola
spring.jpa.properties.hibernate.format_sql=true
